import styled from "styled-components";
import { Container } from "../../components";

const S = {};

const Profile = () => {
  return (
    <Container>
      <p>프로필 페이지임</p>
    </Container>
  );
};

export default Profile;
